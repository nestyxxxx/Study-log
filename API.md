Установка таймера: `setTimeout`- ждёт определённое время, после чего исполняет какой-то код. На вход этот метод принимает:
- первым аргументом колбэк: код, который нужно выполнить;
- вторым аргументом - паузу в миллисекундах, которую нужно выждать, прежде чем исполнять код колбэка;
- аргументы, которые нужно передать колбэку на вход. 
Таймер можно удалять методом `clearTimeout`. Для этого таймер нужно записать в переменную, а затем эту переменную передать методу `clearTimeout`

Циклический таймер: `setInterval` - устанавливает циклический таймер. Он позволяет вызывать колбэк много раз, через заданные промежутки времени. Когда таймер не нужен, его следует удалить, чтобы не тратить ресурсы браузера. Для этого нужно передать таймер методу `clearInterval`

Промис - это часть отдельного API, специально сделанного для работы с асинхронным кодом. У промисов есть три метода:
- `then` - выполнится, если промис исполнен;
- `catch` - если отклонён;
- `finally` - выполнится в любом случае независимо от статуса промиса.
Первые два метода, `then` и `catch`, принимают на вход функцию с одним параметром.

Если вы сразу хотите создать исполненный или отклонённый промис, вызывать `new Promise` необязательно. Можно сразу обратиться к методам `Promise.resolve` и `Promise.reject`. Эти методы создают промис, переводят его в статус «исполнен» или «отклонён» соответственно, и записывают как результат промиса - то, что мы передали этим методам.
Статический метод `Promise.all`. Он принимает на вход массив с промисами и выполняет записанный в `then` код, только когда все промисы вернулись со статусом «исполнен».